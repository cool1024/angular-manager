import { RoleParentPipe } from './role-parent.pipe';

describe('RoleParentPipe', () => {
  it('create an instance', () => {
    const pipe = new RoleParentPipe();
    expect(pipe).toBeTruthy();
  });
});
